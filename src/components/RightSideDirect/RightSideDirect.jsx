import React from "react";
import styled from "styled-components";

const RightSideDirect = () => {
   return <Wrapper>RightSideDirect</Wrapper>;
};

export default RightSideDirect;

const Wrapper = styled.div`
   width: 35rem;
`;
